export struct TunnelRow {
    name: string,
    tunnel-type: string,
    local-ip: string,
    local-port: string,
    remote-port: string,
    remote-addr: string,
    domains: string,
    locations: string,
    basic-auth-user: string,
    basic-auth-password: string,
    host-header-rewrite: string,
    route-by-http-user: string,
    bandwidth-limit: string,
    bandwidth-limit-side: string,
    proxy-protocol-version: string,
    plugin-tls-term: bool,
    plugin-local-addr: string,
    plugin-cert-file: string,
    plugin-key-file: string,
    plugin-host-rewrite: string,
    plugin-username: string,
    plugin-password: string,
}
